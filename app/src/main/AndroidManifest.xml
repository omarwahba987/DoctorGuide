<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahfouz.doctorgp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/gpicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/gpicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SpecialityActivity" />
        <activity android:name=".SearchListDocActivity" />
        <activity android:name=".SymActivity" />
        <activity android:name=".DocNameActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DocProfActivity" />
        <activity android:name=".PatientProfActivity" />
        <activity android:name=".DocPersonalActivity" />
        <activity android:name=".BookActivity" />
        <activity android:name=".AppointmentListActivity" />
        <activity android:name=".AppointmentDetailsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ContactActivity"></activity>
    </application>

</manifest>